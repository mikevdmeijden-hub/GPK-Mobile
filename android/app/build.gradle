plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "nl.gpk.app"
    compileSdk = 34

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        applicationId = "nl.gpk.app"
        minSdk = 23
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            // Voor een deelbare APK volstaat de debug-sleutel (geen betaald
            // certificaat nodig). Mensen zien wel "onbekende bron" bij installeren.
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}
